# VICHA
 vicha is a first class ai chat assistance made unique and easy to understand

 ## Installation

 - make sure you have python install
 - all packages are in the requirement folder but for now you need
 - > Flask = pip install flask
 - > dotenv = pip install dotenv
 - > ai agent = (i use google gemini) pip install google.generativeai
# usage ..